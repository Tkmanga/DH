git add -A
git commit -m "hola pimer cambio por ejemplo"
git remote add origin URL
git remote get-url origin
git push origin master
git pull origin master 
git log
git clone "url"
git status
git checkout "pongo los primeros 7 caracteres del branch que reconocemos con git log al que queremos acceder"
git checkout master = vuelvo al principal
